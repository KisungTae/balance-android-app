package com.beeswork.balance.internal.constant

enum class PhotoStatus {
    EMPTY,
    LOADING,
    DOWNLOADING,
    DOWNLOAD_ERROR,
    UPLOADING,
    UPLOAD_ERROR,
    ORDERING,
    DELETING,
    OCCUPIED
}